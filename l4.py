"""
control flows 
  - conditional statements 
    if....else
    if...elif...else
    ternary operator
    nested if statements
    match case statements
  - loops
  

"""
output = ''
light_status = 'OFF'

if light_status == 'ON':
    output = "turning the lights ON"
else:
    output = "turning the lights OFF"
    
    
    #ternary operator - simplfies conditions into one line of code
    
    
    
    print("======================================")
    print(output)
    print("======================================")